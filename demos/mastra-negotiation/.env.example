# Copy to .env inside this demo directory only. The demo never reads the parent repo .env.
NEGOTIATION_PORT=4317
# Optional: any endpoint implementing the compatible /chat/completions API.
# Live mode remains disabled until all three fields are configured.
NEGOTIATION_BASE_URL=
NEGOTIATION_MODEL=
NEGOTIATION_API_KEY=
