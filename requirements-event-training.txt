# Install CUDA-enabled PyTorch separately using the official selector first.
# Isolate these dependencies from the serving environment; freeze versions after GPU validation.
transformers>=4.46,<6
peft>=0.14,<1
accelerate>=1.2,<2
bitsandbytes>=0.45,<1
pydantic>=2,<3
openai>=1.50,<3
